import React, {createContext, useState} from "react";

export const ChartInfoContext = createContext();
export const VirtualDataContext = createContext();
export const DataChangedContext = createContext();

